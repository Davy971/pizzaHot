{% extends 'base.html.twig' %}

{% block body %}
    <h1>Commande prod list</h1>

    <table>
        <thead>
            
        </thead>
        <tbody>
        {% for comprod in comProd %}
            <tr>
                <li>{{comprod.produit.name}}</li>
				<a href="{{ path('oc_pizzeria_prepar_cuisinier', { 'id': comprod.id }) }}">A préparer</a>
            </tr>
            {% else %}
            Aucune commande actuellement
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
